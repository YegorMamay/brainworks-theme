# DECISIONS

## Формат записи
```
[YYYY-MM-DD] Краткое название решения
Причина:
Последствия:
---
```

## Решения

[2025-02-08] Введение системы живой документации  
Причина: фиксация контекста, решений и задач в документах как единственном источнике истины; долговременная память для ИИ-ассистента.  
Последствия: перед задачами — опора на документы; после — точечное обновление при появлении новой подтверждённой информации; при противоречии диалога и документов приоритет у документации.

---

[2025-02-08] Минимальный набор файлов темы, поддержка PHP 7.4+  
Причина: тема должна быть «голой» и функционировать при минимальных требованиях; совместимость с окружениями на PHP 7.4.  
Последствия: обязательные файлы — style.css, index.php, header.php, footer.php, functions.php; в коде не используются конструкции, требующие PHP 8+.

---

[2025-02-08] Поддержка перевода (Loco Translate) и WooCommerce  
Причина: требование заказчика — возможность перевода темы через Loco Translate и поддержка WooCommerce.  
Последствия: единый text domain brainworks; load_theme_textdomain с путём get_template_directory() . '/languages'; все пользовательские строки через __/esc_html_e с доменом brainworks; папка languages для .po/.mo; в after_setup_theme при class_exists('WooCommerce') объявляется add_theme_support для woocommerce и галереи товаров (zoom, lightbox, slider).

---

[2025-02-08] Стили через препроцессор Sass  
Причина: требование заказчика — писать стили через Sass.  
Последствия: исходники в assets/scss/ (style.scss — главный файл, _variables.scss); сборка: style.scss компилируется в корневой style.css (npm run build:styles); в style.scss в начале блока комментария — метаданные темы для WordPress; в functions.php подключается get_stylesheet_uri() с версией по filemtime.

---

[2025-02-08] Семантическое разбиение стилей по файлам  
Причина: требование заказчика — каждая функциональная часть в отдельном файле, семантичные имена, подключение к основному.  
Последствия: главный файл — style.scss; части: _base.scss (базовые стили, контейнер), _single.scss (страница статьи), _archive.scss (архив блога), _page.scss (статическая страница); при добавлении новых частей — новый частичный файл и @use в style.scss.

---

[2025-02-08] Набор шаблонов страниц темы  
Причина: требование заказчика — все необходимые шаблоны (дефолтная страница, архив, статья, 404, поиск и т.д.).  
Последствия: добавлены page.php, single.php, archive.php, home.php, search.php, 404.php, searchform.php, comments.php, template-parts/content-none.php; index.php использует content-none при отсутствии записей; все строки с доменом brainworks для перевода.

---

[2025-02-08] Области виджетов и шаблоны с боковыми колонками  
Причина: требование заказчика — области виджетов (pre-header, подвал, левая и правая колонки) и шаблоны страниц с боковыми колонками.  
Последствия: в functions.php зарегистрированы четыре области (pre-header, footer, sidebar-left, sidebar-right) с общими before/after widget и widget title; в header.php выведены pre-header (при активных виджетах) и site-header с логотипом/названием; в footer.php — блок подвала с виджетами; sidebar-left.php и sidebar-right.php подключаются через get_sidebar('left')/get_sidebar('right'); добавлены шаблоны «Left Sidebar», «Right Sidebar», «Both Sidebars» (page-sidebar-left/right/both.php) с обёрткой .site-content и grid-раскладкой; стили в _sidebars.scss, адаптив до одной колонки на узких экранах.

---

[2025-02-08] Логотипы в Customizer и шорткоды  
Причина: требование заказчика — в настройках темы (Внешний вид → Настроить → Свойства сайта) возможность добавлять два логотипа (Main Logo, Second Logo), шорткоды для вывода, отдельный файл стилей.  
Последствия: в customize_register добавлены настройки brainworks_main_logo и brainworks_second_logo (WP_Customize_Media_Control) в секции title_tagline; функция brainworks_get_logo_html( which, args ) возвращает HTML логотипа; шорткоды [main_logo] и [second_logo] с атрибутами link, size, class; в шапке выводится Main Logo при наличии, иначе название сайта; стили вынесены в _logos.scss (site-branding, site-logo, site-logo__img). DOCUMENTATION.md создан как руководство по кастомному функционалу; имена шорткодов изменены на main_logo и second_logo по запросу заказчика.

---

[2025-02-08] Раздел Phones в Customizer и шорткод [phones]  
Причина: требование заказчика — в настройках темы раздел «Phones» с 6 полями для номеров; для каждого номера — поле класса (добавляется к тегу <a>) и поле иконки (изображение перед номером); шорткод с форматами вывода (список, колонка, выпадающий список); номера кликабельные (tel:); отображение в формате, заданном пользователем; отдельный файл стилей.  
Последствия: секция brainworks_phones в Customizer с 18 настройками (6 номеров + 6 классов + 6 иконок); brainworks_phone_to_tel() формирует href из цифр с «+»; brainworks_get_phones_data() возвращает массив данных; шорткод [phones] с атрибутами format (list|column|dropdown), class; для dropdown выводится <select> и инлайн-скрипт перехода по tel: при выборе; стили в _phones.scss.

---

[2025-02-08] Раздел Social в Customizer и шорткод [social]  
Причина: требование заказчика — раздел Social с 10 полями для ссылок на соцсети + иконка для каждой; шорткод, выводящий списком в один ряд кликабельные иконки, открывающиеся в новом окне.  
Последствия: секция brainworks_social в Customizer с 20 настройками (10 URL + 10 иконок); brainworks_get_social_data() возвращает массив url/icon_id только для непустых URL; шорткод [social] с атрибутом class, вывод <ul class="social-links"> с <a target="_blank" rel="noopener noreferrer"> и иконкой или плейсхолдером; стили в _social.scss.

---

[2025-02-08] Кнопка «Наверх» (Back to Top Button)  
Причина: требование заказчика — раздел в настройках для управления стилем и положением кнопки возврата наверх; плавный скролл; возможность кастомной стрелки (изображение).  
Последствия: секция brainworks_scroll_to_top в Customizer (Back to Top Button): показ вкл/выкл, положение (bottom-right/left), стиль кнопки (circle/square/rounded), размер (small/medium/large), кастомная стрелка (медиа); вывод кнопки в wp_footer с классами-модификаторами; при отсутствии кастомной стрелки — встроенная SVG-стрелка; assets/js/scroll-to-top.js — видимость после прокрутки 300px, по клику window.scrollTo({ top: 0, behavior: 'smooth' }); стили в _scroll-to-top.scss.

---

[2025-02-08] Поддержка загрузки SVG в медиабиблиотеку  
Причина: требование заказчика — тема должна поддерживать загрузку SVG.  
Последствия: фильтр upload_mimes добавляет image/svg+xml для расширений svg и svgz; фильтр wp_check_filetype_and_ext корректно определяет тип .svg/.svgz; фильтр wp_handle_upload с brainworks_sanitize_svg_upload удаляет из загруженного SVG теги script, атрибуты-обработчики (on*), javascript: и data:*script для снижения рисков.

---

[2026-02-09] Поддержка HTML и описания для телефонов  
Причина: требование пользователя — возможность выделять часть номера жирным (HTML) и указывать имя менеджера рядом с телефоном.  
Последствия: в Customizer для `brainworks_phone_N` разрешён HTML (`wp_kses_post`), добавлено поле `_desc` для описания; `brainworks_phone_to_tel` корректно обрабатывает теги; шорткод `[phones]` выводит описание (класс `.phones__desc`).

---

[2026-02-10] Рефакторинг функций темы
Причина: файл `functions.php` стал слишком большим и сложным для поддержки.
Последствия: код разбит на логические модули в папке `inc/` (`setup.php`, `enqueues.php`, `customizer.php`, `widgets.php`, `shortcodes.php`, `helpers.php` и др.); в `functions.php` остался только вызов `require` для этих файлов; имена функций в `inc/` (кроме `helpers.php` и `template-tags.php`) могут быть без префикса `brainworks_`, если они находятся в пространстве имен или не конфликтуют (в данном случае оставлены глобальные функции).

---

[2026-02-11] Реструктуризация SCSS
Причина: улучшение организации стилей, разделение по компонентам.
Последствия: созданы папки `assets/scss/base`, `blocks`, `elements`, `pages`, `shortcodes`; стили из корня `assets/scss/` перемещены в соответствующие подпапки; `style.scss` обновлен для импорта новых путей.

---

[2026-02-12] Возврат префикса brainworks_ в Customizer
Причина: отказ от рефакторинга имен настроек Customizer для сохранения обратной совместимости и избежания конфликтов.
Последствия: всем настройкам (`add_setting`) и контролам (`add_control`) возвращен префикс `brainworks_` (например, `brainworks_scroll_to_top`, `brainworks_main_logo`).

[2026-02-12] AJAX Добавление в корзину
Причина: требование пользователя — добавление товаров в корзину без перезагрузки страницы (как на архивах, так и в карточке товара).
Последствия: для архивов включена опция WooCommerce `woocommerce_enable_ajax_add_to_cart`; для одиночных товаров написан кастомный JS (`assets/js/ajax-add-to-cart.js`), который перехватывает отправку формы, отправляет AJAX-запрос, обновляет фрагменты корзины и показывает ссылку «Просмотр корзины».

---

[2026-02-12] Поддержка анимированных GIF в товарах
Причина: WordPress по умолчанию ресайзит изображения, что часто ломает анимацию GIF.
Последствия: добавлен хук `image_downsize` в `inc/woocommerce-hooks.php`, который принудительно возвращает полный размер (full) для изображений с MIME-типом `image/gif`, сохраняя анимацию на витрине и в карточке товара.

---

[2026-02-12] Плавный скролл по якорным ссылкам
Причина: улучшение UX и корректное позиционирование при переходе к секциям с учетом высоты плавающей шапки.
Последствия: для `html` добавлено `scroll-behavior: smooth`; в `brainworks.js` добавлен перехват кликов по якорям (`a[href^="#"]`), вычисляющий позицию секции с вычетом высоты шапки и отступа 20px, чтобы контент не перекрывался.

---

[2026-02-12] Поведение закрепленной шапки (Sticky Header)
Причина: шапка должна появляться только после скролла на определенную высоту, и при этом не должен прыгать контент.
Последствия: в `brainworks.js` реализован порог появления (`headerHeight + 200px`); при закреплении (`position: fixed`) к `body` добавляется `padding-top`, равный высоте шапки, чтобы компенсировать ее выпадение из потока документа (устранение «скачка» контента).

---

[2026-02-15] Сортировка секций Customizer
Причина: необходимость менять порядок секций настроек в Customizer без правки кода темы.
Последствия: все кастомные секции в `inc/customizer.php` теперь регистрируются с приоритетом, пропущенным через фильтр `brainworks_customizer_section_priority`. Передаются два аргумента: `$priority` (int) и `$section_id` (string).

---

[2026-02-13] Настройка цветов меню сайдбара
Причина: необходимость отдельной стилизации меню в сайдбаре (`.bw-theme-menu`), отличной от основного меню.
Последствия: в Customizer добавлены настройки для `Sidebar Menu` (Background, Text, Border Color); используются CSS-переменные для применения цветов к виджету меню.

---

[2026-02-14] Улучшенное мобильное меню
Причина: требование сделать меню выезжающим справа, с поддержкой вложенности (аккордеон), закрытием по клику на якорь и кнопкой закрытия.
Последствия: обновлена разметка в `header.php`; JS-логика в `brainworks.js` (открытие/закрытие классами, обработка кликов по стрелкам подменю и якорным ссылкам); стили вынесены в компонент мобильного меню; родительские пункты с пустыми ссылками теперь работают как переключатели подменю.

---

[2026-03-08] Исправление стилей меню сайдбара
Причина: Перезапись фона меню пустыми переменными из Customizer (невалидный CSS `var(--theme-color-1)`), что приводило к потере оригинальных стилей из `.bw-theme-menu`. Установки по умолчанию для цветов сайдбара были жестко заданы на `color_1`, `color_7`, `color_4`, даже если глобальная палитра не настроена.
Последствия: В `inc/customizer.php` дефолтные значения `brainworks_sidebar_menu_bg_color`, `text_color` и `border_color` изменены на `''` (пустая строка). Генерация инлайн CSS для `.bw-theme-menu` сделана условной: свойства выводятся, только если цвет выбран, позволяя применять дефолтные стили из SCSS.

---

[2026-03-08] Двухколоночный макет анонсов статей
Причина: Требование оформить анонсы статей (в архивах, категориях и на главной) в две колонки: слева изображение (обложка), справа — заголовок, мета, отрывок и кнопка "Read More".
Последствия: Обновлена структура цикла вывода постов в шаблонах `archive.php`, `home.php`, `index.php`. Добавлены обертки `.entry-thumbnail` и `.entry-content-wrap`, а также кнопка "Read More". Созданы новые стили для `.post` с использованием Flexbox (ширина колонок установлена на 50% при наличии изображения); при отсутствии изображения контент занимает всю ширину. Изменения CSS вынесены в отдельный файл `_posts.scss`, который импортируется в `style.scss`.

---

[2026-03-08] Пагинация и заголовок на архивных страницах
Причина: Требование сделать красивую пагинацию и отцентрованный заголовок на всех архивных страницах (категории, теги, авторы).
Последствия: Так как в теме для вывода категорий, меток и авторов уже используется единый файл `archive.php`, был добавлен класс `text-center` к обертке `.page-header` в `archive.php` для точного центрирования заголовка и описания по умолчанию. Написаны новые CSS-правила для контейнера пагинации `.pagination` и элементов `.page-numbers` (стилизованы под кнопки с эффектом наведения). Стили добавлены в существующий SCSS-файл `_post-navigation.scss`.

---

[2026-03-08] Исправление хлебных крошек на архивных страницах
Причина: Хлебные крошки генерировали пустой конец ("Home / ") на страницах авторов и датах. Общее условие `is_archive()` срабатывало раньше специфических.
Последствия: В `inc/breadcrumbs.php` изменен порядок проверок условий. Специфические страницы (author, date, category, tag) теперь проверяются *до* общего `is_archive()`. Благодаря этому хлебные крошки правильно выводят имя автора, метку или период архива.

---

[2026-03-08] Заголовок и хлебные крошки на раздельной странице записей (home.php)
Причина: При использовании статической главной страницы, страница записей (`home.php`) не имела заголовка и хлебных крошек.
Последствия: В `home.php` добавлен блок `<header class="page-header text-center"><h1 class="page-title"><?php single_post_title(); ?></h1></header>`. В генераторе `breadcrumbs.php` ранний возврат был изменен с `if (is_front_page() || is_home())` на `if (is_front_page())`, а также добавлено специальное условие `if (is_home() && !is_front_page())` для вывода названия страницы записей в цепочку хлебных крошек.

---

[2026-03-26] Исправление настройки липкого сайдбара (Sticky Sidebar)
Причина: Сайдбар оставался в состоянии `position: sticky` даже при отключении опции в Customizer, так как свойство было жестко задано в SCSS.
Последствия: В `assets/scss/blocks/_sidebars.scss` значение `position: sticky` заменено на использование CSS-переменной `var(--sidebar-sticky, static)`. Теперь при отключении чекбокса в настройках переменная принимает значение `static`, и сайдбар ведет себя как обычный блок.

---

[2026-04-01] Шаблон страницы входа и регистрации с фронтенда  
Причина: Требование сделать отдельную страницу с формами входа и регистрации пользователя без перехода в `wp-login.php`.  
Последствия: Добавлен шаблон `page-auth.php` (Template Name: Login & Register) с фронтенд-обработкой входа (`wp_signon`) и регистрации (`wp_create_user`), проверкой nonce и валидацией полей; при успешном входе выполняется редирект на главную. Добавлены стили в `assets/scss/pages/_auth.scss` и подключение в `assets/scss/style.scss`.

